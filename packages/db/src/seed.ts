/**
 * Canonical admin seed: repository root `scripts/seed.ts`.
 * Run `pnpm seed` or `pnpm seed:remote` from the monorepo root (not this package).
 */
console.error(
  'Use `pnpm seed` from the repository root (see scripts/seed.ts).',
);
process.exit(1);
